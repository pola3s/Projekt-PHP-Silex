{% extends 'layout.twig' %}

{% block content %}
    {% for file in files %}
        <hr>
        <h2 class="intro-text text-center">
            {{ file.title|e }}
        </h2>
        <hr>
        <div class="col-lg-12 text-center">
            <p>
                <img class="img-responsive img-full" src="{{ app.request.basepath }}/media/{{ file.name }}">
               
            </p><a href="{{ url('/files/delete',{'name': file.name|e}) }}"
                   title="Delete Photo">
                <button type="button" class="btn btn-danger">Usuń Photo
                </button>
            </a></p>
          
            </p>
            <hr>
        </div>
    {% endfor %}

{% endblock %}